-- [[ ELERNATE HUB - NEW ICON UPDATE ]] --

local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local lp = Players.LocalPlayer

-- Asset Configuration Baru
local LogoID = "rbxassetid://104493729902121" -- ID Ikon Baru
local AccentColor = Color3.fromRGB(255, 0, 0)

-- Clean Up
if CoreGui:FindFirstChild("ELERNATE_REBORN") then
    CoreGui:FindFirstChild("ELERNATE_REBORN"):Destroy()
end

local ScreenGui = Instance.new("ScreenGui", CoreGui)
ScreenGui.Name = "ELERNATE_REBORN"
ScreenGui.IgnoreGuiInset = true

-- [[ 1. INTRO LOADING CLEAN ]] --
local function PlayIntro()
    local IntroFrame = Instance.new("Frame", ScreenGui)
    IntroFrame.Size = UDim2.new(1, 0, 1, 0)
    IntroFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    IntroFrame.ZIndex = 100
    
    local LoadTxt = Instance.new("TextLabel", IntroFrame)
    LoadTxt.Text = "Loading..."
    LoadTxt.Font = Enum.Font.GothamBold
    LoadTxt.TextSize = 30
    LoadTxt.TextColor3 = Color3.fromRGB(255, 255, 255)
    LoadTxt.Position = UDim2.new(0.5, 0, 0.5, 0)
    LoadTxt.AnchorPoint = Vector2.new(0.5, 0.5)
    LoadTxt.BackgroundTransparency = 1
    
    wait(0.8)
    TweenService:Create(IntroFrame, TweenInfo.new(1), {BackgroundTransparency = 1}):Play()
    TweenService:Create(LoadTxt, TweenInfo.new(0.5), {TextTransparency = 1}):Play()
    wait(1)
    IntroFrame:Destroy()
end

-- [[ 2. TOGGLE BUTTON (IKON BARU - NO BG) ]] --
local ToggleBtn = Instance.new("ImageButton", ScreenGui)
ToggleBtn.Name = "Toggle"
ToggleBtn.Size = UDim2.new(0, 50, 0, 50)
ToggleBtn.Position = UDim2.new(1, -70, 0, 120)
ToggleBtn.BackgroundTransparency = 1 -- Background dihapus total
ToggleBtn.Image = LogoID
ToggleBtn.ScaleType = Enum.ScaleType.Fit

-- [[ 3. MAIN GUI GLASSMORPHISM ]] --
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 480, 0, 320)
MainFrame.Position = UDim2.new(0.5, -240, 0.5, -160)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BackgroundTransparency = 0.25
MainFrame.Visible = false
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

-- Label Nama ELERNATE di Kiri Atas
local HubTitle = Instance.new("TextLabel", MainFrame)
HubTitle.Text = "ELERNATE"
HubTitle.Font = Enum.Font.GothamBold
HubTitle.TextSize = 18
HubTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
HubTitle.Position = UDim2.new(0, 15, 0, 15)
HubTitle.Size = UDim2.new(0, 120, 0, 25)
HubTitle.TextXAlignment = Enum.TextXAlignment.Left
HubTitle.BackgroundTransparency = 1

-- Sidebar
local Sidebar = Instance.new("Frame", MainFrame)
Sidebar.Position = UDim2.new(0, 0, 0, 55)
Sidebar.Size = UDim2.new(0, 140, 1, -55)
Sidebar.BackgroundTransparency = 1

local Tab = Instance.new("TextButton", Sidebar)
Tab.Size = UDim2.new(1, -20, 0, 35)
Tab.Position = UDim2.new(0, 10, 0, 0)
Tab.BackgroundColor3 = AccentColor
Tab.Text = "UNIVERSAL"
Tab.Font = Enum.Font.GothamMedium
Tab.TextColor3 = Color3.fromRGB(255, 255, 255)
Tab.TextSize = 13
Instance.new("UICorner", Tab).CornerRadius = UDim.new(0, 5)

-- Content Scroll
local Scroll = Instance.new("ScrollingFrame", MainFrame)
Scroll.Position = UDim2.new(0, 150, 0, 50)
Scroll.Size = UDim2.new(1, -160, 1, -60)
Scroll.BackgroundTransparency = 1
Scroll.ScrollBarThickness = 0
local Layout = Instance.new("UIListLayout", Scroll)
Layout.Padding = UDim.new(0, 10)

-- [[ 4. FUNCTIONAL FEATURES ]] --
local function AddToggle(name, callback)
    local btn = Instance.new("TextButton", Scroll)
    btn.Size = UDim2.new(1, -5, 0, 40)
    btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    btn.BackgroundTransparency = 0.92
    btn.Text = "  " .. name
    btn.Font = Enum.Font.Gotham
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 13
    btn.TextXAlignment = Enum.TextXAlignment.Left
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    
    local Indicator = Instance.new("Frame", btn)
    Indicator.Size = UDim2.new(0, 8, 0, 8)
    Indicator.Position = UDim2.new(1, -20, 0.5, -4)
    Indicator.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    Instance.new("UICorner", Indicator).CornerRadius = UDim.new(1, 0)
    
    local active = false
    btn.MouseButton1Click:Connect(function()
        active = not active
        Indicator.BackgroundColor3 = active and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
        btn.BackgroundTransparency = active and 0.75 or 0.92
        callback(active)
    end)
end

-- Anti-AFK Sangat Maksimal
AddToggle("Anti-AFK (Ultimate Protect)", function(v)
    _G.AntiAFK = v
    spawn(function()
        while _G.AntiAFK do
            local VirtualUser = game:GetService("VirtualUser")
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
            wait(30)
        end
    end)
end)

AddToggle("Infinite Jump", function(v)
    _G.InfJump = v
    UserInputService.JumpRequest:Connect(function()
        if _G.InfJump and lp.Character:FindFirstChildOfClass("Humanoid") then 
            lp.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping") 
        end
    end)
end)

AddToggle("Noclip", function(v)
    _G.Noclip = v
    RunService.Stepped:Connect(function()
        if _G.Noclip and lp.Character then
            for _, p in pairs(lp.Character:GetDescendants()) do
                if p:IsA("BasePart") then p.CanCollide = false end
            end
        end
    end)
end)

AddToggle("ESP Names", function(v)
    _G.ESP = v
    spawn(function()
        while _G.ESP do
            for _, plr in pairs(Players:GetPlayers()) do
                if plr ~= lp and plr.Character and not plr.Character:FindFirstChild("Highlight") then
                    Instance.new("Highlight", plr.Character).FillColor = AccentColor
                end
            end
            wait(1)
            if not _G.ESP then
                for _, plr in pairs(Players:GetPlayers()) do
                    if plr.Character and plr.Character:FindFirstChild("Highlight") then 
                        plr.Character.Highlight:Destroy() 
                    end
                end
            end
        end
    end)
end)

-- [[ 5. DRAG & TOGGLE LOGIC ]] --
ToggleBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)

local drag, dInput, dStart, sPos
MainFrame.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 then drag = true dStart = i.Position sPos = MainFrame.Position end
end)
UserInputService.InputChanged:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseMovement and drag then
        local delta = i.Position - dStart
        MainFrame.Position = UDim2.new(sPos.X.Scale, sPos.X.Offset + delta.X, sPos.Y.Scale, sPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then drag = false end end)

PlayIntro()
MainFrame.Visible = true